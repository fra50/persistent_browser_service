version: '3.8'
services:
  pb-service:
    build: .
    container_name: pb-service
    ports:
      - "${API_PORT:-8081}:4000"
      - "${NOVNC_PORT:-6080}:7900"
      - "${VNC_PORT:-5901}:5900"
    env_file:
      - .env
    volumes:
      - ./pb-profiles:/profiles
    restart: unless-stopped
